import { Game } from "../engine/core/game";

/**
 * A recreation of the popular AdVenture Capitalist web game.
 */
export class AdVentureCapitalist extends Game {

    /**
     * Callback for when the game is loaded.
     * @param gameData The game's configured data.
     */
    public onGameLoad(gameData: any): void {

    }
}