/**
 * The various asset types which the {@link AssetLoader} can handle loading.
 */
export enum AssetType {

    /**
     * A sprite asset, used to display basic images.
     */
    SPRITE = "sprite"
}